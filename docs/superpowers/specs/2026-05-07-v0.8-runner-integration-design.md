# v0.8 Runner Integration — Design Spec

**Status:** approved 2026-05-07 (brainstorming session)
**Branch:** `cliq/v0.8-runner-integration`
**Parent:** `cliq/v0.7-tx-runtime-spec` (53 commits implementing Phases 1–14 of the v0.8 plan)
**Reference:** OpenAI Codex CLI `SessionTask` / `ToolOrchestrator` patterns (see Architecture §A.1).

## Goal

Wire the transactional workspace runtime (already implemented as a standalone module) into the runner's turn lifecycle so `cliq run` and `cliq "..."` actually use it. Today the coordinator works only against manually-constructed approved transactions. After this change:

- A user running `cliq --tx edit "fix the foo bug"` gets an implicit per-turn tx that opens at turn start, accumulates edits in an overlay, validates at turn end, and applies if `applyPolicy` permits.
- `cliq tx open foo` followed by multiple turns accumulates edits across turns into one explicit tx, with apply driven by `cliq tx apply <id>`.
- bash tool runs under the configured `bashPolicy` and records `BashEffect`s to the active tx.
- `cliq tx validate <id>` and `cliq tx approve <id>` complete the manual workflow.
- Crash recovery converges leftover transactions at runner startup.

## Scope

**In scope:**
1. Runner integration (open at turn start; finalize → validate → approve → apply at turn end).
2. Coordinator API additions: `getActiveTx`, `finalizeTx`, `validateTx`, `approveTx`, `recoverAtStart` — all function-style (§A.10a).
3. Runtime error event contract: optional `code?: HeadlessErrorCode` and `recoverable?: boolean` on the `error` variant of `RuntimeEvent`; headless mapper honors them (§A.10b).
4. CLI subcommands `cliq tx validate <id>`, `cliq tx approve <id> [--override <name>] [--override-all] [--allow-validator-error <name>]`. Existing `cliq tx apply <id>` becomes state-aware: walks the tx forward through finalize/validate/approve/apply for whichever stages are missing.
5. Bash policy enforcement at the bash tool's `execute()` site, with `BashEffect` recording.
6. Tests at the unit and integration levels described in the Testing section, including direct headless event-mapping tests for the new error contract.

**Out of scope** (future polish PR):
- `cliq tx diff/show/validators/help` subcommands.
- Stage C crash-convergence e2e integration test.
- AB3a.5 race injection test.
- Stage B B3b disk-error explicit test.
- `cliq run --jsonl` subprocess-based end-to-end test.
- Pre-existing `extensions/loader.test.ts` /tmp/workspace flake.

## Non-goals

- Backward-compatibility invariant. The runner integration may require updates to existing v0.7 callsites and tests; we don't bend the API to preserve byte-for-byte v0.7 behavior. Tests get updated along with the code.

## Architectural Decisions

### A.1 — Tx orchestration is a direct parameter on `createRunner`

Codex CLI's `SessionTask` (codex-rs/core/src/tasks/regular.rs) wraps `run_turn` as a coordinator-above-loop layer. We mirror this with an optional `transactions?: TxRunnerOptions` parameter on `createRunner`. When provided, `runTurn` extracts a turn lifecycle that wraps the existing model+tools body.

Rejected alternatives:
- Hooks (`RuntimeHook.beforeTurn` / `afterTurn`): the existing pipeline swallows hook errors via `console.error`; tx lifecycle errors must propagate.
- Wrapper factory `createTxRunner` over an inner `createRunner`: writer injection needs to share state with tool dispatch, which lives on the inner runner — wrapper has to monkey-patch the registry.

### A.2 — `applyPolicy=interactive` + headless = refuse at session start

Codex CLI hardcodes `AskForApproval::Never` for `codex exec` (codex-rs/exec/src/lib.rs:404). It does not promote-mid-run. We follow the same stance:

> When `transactions.applyPolicy === 'interactive'` and `headless === true`, `createRunner` throws at construction with a clear message: `--tx-apply interactive requires a TTY; use --tx-apply manual-only or auto-on-pass for headless runs`.

This intentionally diverges from `bashPolicy=confirm`, which we keep as "promote to deny under headless." `bashPolicy` is per-call (model can adapt to a denied bash); `applyPolicy` is per-run (set once, hard to recover from mid-turn).

### A.3 — `enforceBashPolicy` lives inside the bash tool's `execute()`

The bash tool reads `context.tx` (see A.5) and:
1. Calls `enforceBashPolicy({ policy: tx.bashPolicy, txMode: tx.mode, headless: tx.headless, confirm? })`.
2. On `deny` → returns `{ status: 'error', meta: { code: 'tx-overlay-error', error: message } }`.
3. On `allow` → snapshots cwd mtimes, runs bash, snapshots again, calls `tx.recordBashEffect(eff)` with the diff.

Rejected alternative: dispatch-level enforcement (Codex's `ToolOrchestrator` pattern). bashPolicy is a single-tool concern; localizing avoids piling tx-specific logic onto the runner's tool dispatch.

### A.4 — Coordinator builds the validator registry on demand inside `validateTx`

`coordinator.validateTx(ctx, txId, validatorsConfig, stagedViewConfig)` calls `buildValidatorRegistry(validatorsConfig)`, materializes the staged view from `stagedViewConfig`, runs `runValidators`, persists each result via `onResult: writeValidatorResult(root, txId, result)`, cleans up the staged view, and updates `tx.validators` + `tx.blockingFailures`. One method, one pipeline. Caller (CLI handler or `tx-runner.ts`) is responsible for reading workspace config and passing the relevant blocks (§A.10a — coordinator stays stateless).

Rejected alternatives:
- Runner builds registry at construction: couples runner to validator config.
- Caller passes registry per-call: pushes work onto every callsite (manual `cliq tx validate` would have to build it too).

### A.5 — `ToolContext.tx?: { mode, bashPolicy, txId, headless, recordBashEffect }`

A single optional namespace, presence/absence cleanly signals tx-on/off. Future tx-aware tools (e.g., a search tool that prefers overlay content) read from the same struct without growing `ToolContext` per-feature.

```ts
export type ToolContext = {
  cwd: string;
  session: Session;
  signal?: AbortSignal;
  writer?: WorkspaceWriter; // existing — Phase 3
  tx?: ToolContextTxFacade;
};

export type ToolContextTxFacade = {
  mode: 'edit';
  bashPolicy: TxBashPolicy;
  txId: string;
  headless: boolean;
  recordBashEffect(eff: BashEffect): Promise<void>;
};
```

### A.6 — Recovery runs at CLI entry, not at runner construction

`createRunner` stays synchronous. Recovery is async file IO and does not belong in a sync constructor; making the constructor async would force significant callsite churn.

Instead, recovery is invoked from CLI entry points before any runner work:

- **`cliq` (the model-loop entry)**: every site that constructs a runner awaits `recoverAtStart(ctx)` once after session resolution and before `createRunner`. Today there are exactly two such sites: `src/headless/run.ts` (~line 233, used by both the `--jsonl` and non-jsonl one-shot CLI arms via `runHeadless`) and `src/cli.ts` (~line 1565, the interactive REPL `chat` arm). The two `runHeadless` callsites in `src/cli.ts` (~line 1491 jsonl, ~line 1516 non-jsonl streaming) do NOT construct runners themselves — they call `runHeadless` and the runner lives there. Errors from `recoverAtStart` propagate as a `ReportedCliError` with exit code 2.
- **Every `cliq tx <subcommand>` handler**: the handler awaits `coordinator.recoverAtStart(ctx)` first, then performs its operation. Same error semantics. This catches orphans even when the user invokes only tx commands without running the model loop.

`recoverAtStart(ctx)` is idempotent (no-op when nothing to do) and cheap (scan `<txRoot>/tx_*` dirs, reread state.json). Calling it from both entry points means orphans never accumulate silently regardless of how the user re-enters.

Cross-session guard: tx artifacts carry `tx.sessionId`. `recoverAtStart` filters to `tx.sessionId === ctx.session.id`. Cross-session orphans get a `recovery.json` with `action: 'cross-session-skipped'`; the entry point emits one runtime warning event per skip so JSONL consumers see them. No state mutation happens across session boundaries in v0.8.

### A.7 — `applyPolicy=manual-only` + `auto: per-turn` is refused at config-load

The combination "auto-open every turn but never auto-apply" produces dangling tx artifacts every turn with no clear lifecycle. We refuse it at workspace config validation time:

> `transactions.applyPolicy=manual-only requires transactions.auto=manual; per-turn auto-open with manual-only apply produces dangling transactions.`

Same error shape if the CLI override is invalid: `--tx-apply manual-only` requires `--tx edit` to be paired with explicit `cliq tx open` (auto: manual at runtime).

### A.8 — On `applyPolicy=interactive` user-no, auto-abort at turn end

When the prompt at turn end returns no, runner calls `coordinator.abortTx({ reason: 'user-abort' })` instead of leaving the tx pending. Symmetric with `auto-on-pass` which closes the tx at turn end either way.

### A.9 — `BashEffect` persistence: append-only JSONL

`<txDir>/bash-effects.json` (JSONL, one effect per line). Helpers `appendBashEffect(root, txId, eff)` and `readBashEffects(root, txId)` mirror `appendAudit`/`readAudit` from store.ts. Cleanest lifecycle: independent of apply-progress, captures effects from staging time forward, no contention with state.json.

### A.10a — Coordinator stays function-style; new exports listed explicitly

Existing `src/workspace/transactions/coordinator.ts` is function-style: `openTx(ctx, opts)`, `getTxStatus(ctx, txId)`, `listTx(ctx)`, `applyTx(ctx, txId, opts)`, `abortTx(ctx, txId, opts)`. We do NOT refactor to an object/class with cached state. New exports added to the same module:

| Function | Signature | Behavior |
|---|---|---|
| `getActiveTx` | `(ctx: CoordinatorContext) => Promise<Transaction \| null>` | Returns `readTxState(root, ctx.session.activeTxId)` if set, else null. Pure read. |
| `finalizeTx` | `(ctx, txId) => Promise<{ diffSummary }>` | Computes diff from overlay, writes diff.json, transitions tx → 'finalized'. Throws on IO failure. |
| `validateTx` | `(ctx, txId, validatorsConfig: TxValidatorsConfig, stagedViewConfig: TxStagedViewConfig) => Promise<{ validators, blockingFailures }>` | Builds registry, materializes staged view, runs validators, persists results, transitions → 'validated'. Validator config passed in by caller (CLI handler reads workspace config; runner's tx-runner reads from TxRunnerOptions). No coordinator-side caching. |
| `approveTx` | `(ctx, txId, opts: { overrides?: string[]; overrideAll?: boolean; allowValidatorError?: string[]; reason?: string }) => Promise<{ ok: true } \| { ok: false; uncoveredFailures: string[] }>` | If blocking failures are uncovered, returns `{ ok: false, uncoveredFailures }`. Else transitions → 'approved'. |
| `recoverAtStart` | `(ctx) => Promise<{ recovered: RecoveryOutcome[]; crossSessionSkipped: string[] }>` | Wraps `recoverAll` with the cross-session filter (A.6). Returns own-session recovery outcomes plus the list of cross-session txIds skipped. Caller is responsible for emitting runtime events for each. |

`validatorsConfig` and `stagedViewConfig` flow through call arguments, not coordinator-cached state. This keeps the module stateless (mirrors how `applyTx`/`abortTx` already accept all needed options per call). The runner's `TxRunnerOptions` still carries these (so `tx-runner.ts` doesn't have to re-read workspace config every turn), but coordinator itself never reads workspace config.

### A.10b — Runtime error event carries optional code + recoverable

The current `RuntimeEvent` error variant is `{ type: 'error'; stage: 'model'|'protocol'|'policy'|'tool'|'cancel'; message: string }`. The headless mapper hardcodes `recoverable: false`. To represent `tx-apply-conflict` (recoverable), `tx-apply-partial` (not recoverable), `tx-overlay-error`, and cross-session-skipped warnings as documented elsewhere in this spec, the error contract grows:

```ts
// src/runtime/events.ts
| {
    type: 'error';
    stage: 'model' | 'protocol' | 'policy' | 'tool' | 'cancel';
    message: string;
    code?: HeadlessErrorCode;       // NEW — optional override of the default code-by-stage mapping
    recoverable?: boolean;          // NEW — defaults to false if omitted
  };
```

The headless mapper (`src/headless/events.ts:runtimeEventToHeadless`) is updated:

- `code`: if `event.code` is set, use it; otherwise fall back to the existing `codeByRuntimeErrorStage[event.stage]` table.
- `recoverable`: if `event.recoverable` is set, use it; otherwise default to `false` (preserves current behavior).

This is purely additive on the producer side: existing emitters that don't set `code`/`recoverable` continue to map to their current shape. Tx-aware emitters (runner end-of-turn, CLI tx subcommand handlers, recovery entry) set both fields explicitly.

## Components

### Modified files

| File | Change |
|---|---|
| `src/runtime/runner.ts` | `createRunner` accepts `transactions?: TxRunnerOptions` (sync constructor; recovery is the CLI entry's responsibility per §A.6). Detects headless+interactive collision at construction. Delegates turn lifecycle to `tx-runner`. Tool dispatch site builds `ToolContext.tx` and `ToolContext.writer` from runner options + `coordinator.getActiveTx(ctx)`. |
| `src/runtime/events.ts` | Adds optional `code?: HeadlessErrorCode` and `recoverable?: boolean` to the error variant of `RuntimeEvent` (§A.10b). Adds the 5 tx event variants if not already present from Phase 11. Runner becomes a producer. |
| `src/headless/events.ts` | `runtimeEventToHeadless` honors `event.code` and `event.recoverable` overrides (§A.10b). Defaults preserved. |
| `src/workspace/transactions/coordinator.ts` | Adds `finalizeTx`, `validateTx`, `approveTx`, `recoverAtStart`, `getActiveTx`. Function-style, no cached state (§A.10a). |
| `src/tools/types.ts` | `ToolContext` adds `tx?: ToolContextTxFacade`. |
| `src/tools/bash.ts` | Reads `context.tx`. When present: enforces policy, records BashEffect. |
| `src/cli.ts` | Adds `tx-validate` and `tx-approve` to `ParsedArgs`; parser branches; runCli handlers. CLI handlers call `recoverAtStart` before any other coordinator method. |
| `src/headless/contract.ts` | `HeadlessRunRequest` grows optional tx override fields: `txMode?: 'off' \| 'edit'`, `txApply?: 'interactive' \| 'auto-on-pass' \| 'manual-only'`. Both default to `undefined` (use workspace config). Without these fields the CLI's parsed `--tx`/`--tx-apply` flags would be silently dropped on the `runHeadless` boundary. |
| `src/headless/run.ts` (`createRunner` ~L233; only runner-construction site for headless paths) | Awaits `recoverAtStart(ctx)` after session resolution and before `createRunner`. Resolves `txMode` and `txApply` from `request.txMode ?? cfg.transactions.mode` and `request.txApply ?? cfg.transactions.applyPolicy`. When `txMode === 'edit'`, builds `TxRunnerOptions` and passes via `transactions:` to `createRunner`. Sets `headless: true` (this layer is always headless; refuses interactive applyPolicy at construction per §A.2). Emits a runtime warning event per cross-session-skipped txId via the existing event sink. |
| `src/cli.ts` (interactive REPL `chat` arm, `createRunner` ~L1565) | Awaits `recoverAtStart(ctx)` after `ensureSession` and before `createRunner`. Builds `transactions` from workspace config + `parsed.txMode`/`parsed.txApply` overrides. Sets `headless: false` (interactive arm). Tx subcommand handlers (`tx-validate`, `tx-approve`, etc.) also call `recoverAtStart` at handler entry; they read workspace config directly (no runner involved). |
| `src/cli.ts` (one-shot prompt arms invoking `runHeadless` ~L1491 jsonl, ~L1516 streaming) | Forward `txMode: parsed.txMode` and `txApply: parsed.txApply` into the `HeadlessRunRequest`. Without this update, `cliq run --jsonl --tx edit "..."` parses the flags but builds a non-tx runner inside `runHeadless`. |

### New files

| File | Purpose |
|---|---|
| `src/runtime/tx-runner.ts` | Owns turn-lifecycle helpers. Exports `openTurnTx(ctx, opts) → { tx, writer }`, `finishTurnTx(ctx, opts, signal)`, and the construction-time `assertHeadlessCompatible(opts)` check. Keeps runner.ts focused on model+tools. |
| `src/runtime/tx-runner.test.ts` | Unit tests for the above. |
| `src/workspace/transactions/bash-effects.ts` | `appendBashEffect`, `readBashEffects`. |
| `src/workspace/transactions/bash-effects.test.ts` | JSONL append/read tests. |

### TxRunnerOptions

```ts
export type TxRunnerOptions = {
  mode: 'edit';                                   // tx mode (config + CLI override)
  auto: 'per-turn' | 'manual';
  applyPolicy: 'interactive' | 'auto-on-pass' | 'manual-only';
  bashPolicy: TxBashPolicy;
  headless: boolean;
  validatorsConfig: TxValidatorsConfig;           // passed to coordinator.validateTx by tx-runner
  stagedViewConfig: TxStagedViewConfig;           // for staged view materialization
  workspaceId: string;
  workspaceRealPath: string;
  cliqHome?: string;                              // forwarded into the per-call CoordinatorContext
  confirmApply?: () => Promise<boolean>;          // TTY prompt callback for applyPolicy=interactive
};
```

`tx-runner.ts` imports the coordinator's stage functions directly (`import { openTx, getActiveTx, finalizeTx, validateTx, approveTx, applyTx, abortTx } from '../workspace/transactions/coordinator.js'`) — there is no coordinator object. This matches §A.10a's stateless-function-style decision and avoids implying a TxCoordinator class. Tests that need to stub stages can use Node's `--experimental-vm-modules` mock support or, if more isolation is needed later, we can refactor `tx-runner` to accept a `stages?: { openTx?, finalizeTx?, ... }` injection bundle. v0.8 keeps it simple with direct imports.

## Data flow

### Per-turn implicit tx (auto-on-pass, the most common path)

```text
runTurn(session, userInput)
├─ tx-runner.openTurnTx(ctx, opts)
│  ├─ coordinator.openTx({ explicit: false }) → tx, sets activeTxId, NO tx-opened record
│  └─ emit 'tx-staging-start' { txId, trigger: 'auto-turn' }
├─ build OverlayWriter for tx (createOverlayWriter(cwd, <txDir>/overlay))
├─ existing turn body runs:
│  ├─ checkpoint, model loop, tool dispatch
│  └─ each definition.execute(action, { cwd, session, signal, writer, tx: facade })
├─ tx-runner.finishTurnTx(ctx, opts, signal)
│  ├─ coordinator.finalizeTx(txId)
│  │  ├─ computeDiff(cwd, overlayRoot) → write diff.json
│  │  ├─ tx.diffSummary = summarizeDiff(diff)
│  │  ├─ state='finalized'
│  │  └─ emit 'tx-finalized' { diffSummary }
│  ├─ coordinator.validateTx(txId)
│  │  ├─ buildValidatorRegistry(cfg.transactions.validators)
│  │  ├─ materializeStagedView(cwd, overlayRoot, stagedTarget, bindPaths, copyMode)
│  │  ├─ runValidators({ txId, registry, workspaceView: stagedTarget, realCwd: cwd, onResult: persist })
│  │  ├─ cleanupStagedView(stagedTarget)
│  │  ├─ tx.validators = results, tx.blockingFailures = blocking-failures
│  │  ├─ state='validated'
│  │  └─ emit 'tx-validated' { validators, blockingFailures }
│  ├─ if applyPolicy='auto-on-pass' and blockingFailures.length === 0:
│  │  └─ coordinator.approveTx(txId) → state='approved'
│  ├─ if applyPolicy='interactive':
│  │  ├─ const ok = await opts.confirmApply!()
│  │  ├─ if !ok: coordinator.abortTx({ reason: 'user-abort' }) → emit 'tx-aborted'; return
│  │  └─ coordinator.approveTx(txId)
│  ├─ coordinator.applyTx(txId)
│  │  ├─ runApplyTx (Stage A→B→C)
│  │  ├─ session.records ← tx-applied (deterministic id)
│  │  ├─ activeTxId cleared
│  │  └─ emit 'tx-applied' { ... }
│  └─ cleanupOverlay(<txDir>/overlay)
└─ return finalMessage
```

### Branch points at end-of-turn

- **Blocking validator failures, no overrides**: `coordinator.approveTx` returns rejected; runner calls `coordinator.abortTx({ reason: 'validator-fail' })`; emit 'tx-aborted'. No edits land.
- **`applyPolicy='manual-only'` (auto: manual only, per A.7)**: not reachable on auto-per-turn path. On explicit tx, runner doesn't auto-finalize at turn end; user drives `cliq tx validate/approve/apply`.
- **`ApplyConflict` from `applyTx`**: result `{ ok: false, error: 'conflict' }`; runner emits standard 'error' event with code='tx-apply-conflict' (recoverable=true) only. Tx stays 'approved' (Stage A never wrote progress). User can retry `cliq tx apply <id>` after resolving the external change. We intentionally do NOT emit `tx-aborted` here — the tx is not in aborted state; emitting would mislead consumers.
- **`ApplyPartial` from `applyTx`**: result `{ ok: false, error: 'partial' }`; runner emits 'error' with code='tx-apply-partial' (recoverable=false). Tx stays 'applied-partial'. The user must run `cliq tx abort --restore-confirmed | --keep-partial`.
- **`coordinator.finalizeTx` or `coordinator.validateTx` throws** (not validator-fail status, but actual exception): runner calls `coordinator.abortTx({ reason: 'staging-error' })`; validators that completed are persisted; emit 'tx-aborted'.
- **Workspace-not-git** (snapshot creation fails inside Stage A): `runApplyTx` throws; caught at `finishTurnTx`; tx stays 'approved'; emit 'error' code='tx-overlay-error'.

### Explicit tx flow

User invoked `cliq tx open <name>` (or workspace config has `auto: manual`).

- The `tx-staging-start` event fires once per tx open, from `coordinator.openTx`. The CLI `tx-open` handler emits it on explicit open; the runner's auto-open path emits it on implicit per-turn open. Subsequent turns reusing an existing explicit tx do NOT re-emit.
- Runner finds existing `activeTxId` at turn start. Calls `coordinator.getActiveTx(ctx)` to fetch (added in §A.10a). If state is `staging`, reuses the existing overlay; runs the turn body; at turn end does NOT finalize/validate/apply (that's user-driven).
- Edits accumulate in the overlay across turns.
- User drives the lifecycle via `cliq tx validate <id>`, `cliq tx approve <id>`, `cliq tx apply <id>`.

**`cliq tx apply <id>` chains stages by current state.** For UX, the apply handler is state-aware: it walks the tx forward through whichever stages are missing rather than failing with "tx state is staging; cannot apply." Behavior:

| Current state | apply handler does |
|---|---|
| `staging` | `finalizeTx` → `validateTx` → if no blocking failures, `approveTx` → `applyTx` |
| `finalized` | `validateTx` → if no blocking failures, `approveTx` → `applyTx` |
| `validated` | if no blocking failures, `approveTx` → `applyTx`; if blocking failures, error and tell user to use `cliq tx approve --override <name>` |
| `approved` | `applyTx` directly |
| `applied` / `aborted` / `applied-partial` | error: not in an applyable state |

Each stage emits its own runtime event. If any stage throws (or returns error), the chain stops and the error surfaces — user can drive remaining stages manually. `cliq tx validate` and `cliq tx approve` remain available for granular control (e.g., user wants to inspect validators before approving). The handler honors `applyPolicy` for the final apply step the same way the runner does (interactive prompts via TTY, refuses headless+interactive at command entry).

This keeps the coordinator API 1:1 (one method per stage) while the CLI provides a smart-pipeline UX for the common case.

### Recovery at runner startup

`coordinator.recoverAtStart(ctx)`:

```text
const orphans = await scanForRecovery(root)
const ownSession = orphans.filter(a => a.tx.sessionId === ctx.session.id)
const crossSession = orphans.filter(a => a.tx.sessionId !== ctx.session.id)
for orphan in ownSession: recoverApply or recoverAbort
for orphan in crossSession: writeRecoveryRecord(root, { txId, action: 'cross-session-skipped', warning: 'tx originated in a different session', ts })
emit one 'error' event per cross-session skip (recoverable=true, message includes txId)
```

Same logic runs at the start of every CLI `tx` subcommand handler.

## Error handling

| Source | Channel | Effect |
|---|---|---|
| `recoverAtStart` failure (IO error reading tx dir) | thrown from CLI entry (before `createRunner`, per §A.6) | CLI exits with `ReportedCliError({ exitCode: 2 })` |
| `coordinator.openTx` failure | thrown from `runTurn` | Turn aborts before model runs |
| `OverlayWriter.replaceText` failure | tool returns `{ status: 'error' }` | Model sees error, can adapt |
| `bashPolicy=deny` rejection | tool returns `{ status: 'error', meta.code: 'tx-overlay-error' }` | Model sees error |
| `coordinator.finalizeTx`/`validateTx` exception | runner catches, calls `abortTx({ reason: 'staging-error' })` | Tx aborted; emit 'tx-aborted' |
| Validator returns `status: 'fail'` (blocking) | normal flow | `approveTx` blocks; runner aborts with reason='validator-fail' unless overrides cover |
| `ApplyConflict` | structured `applyTx` result | Tx stays 'approved'; emit 'error' code='tx-apply-conflict' only (no 'tx-aborted' — tx is not aborted, user can retry) |
| `ApplyPartial` | structured result | Tx stays 'applied-partial'; emit 'error' code='tx-apply-partial' recoverable=false |
| Cancellation (`signal.aborted`) | existing throwIfCancelled | Cancellation throw; recovery converges next run |

`runHooks` keeps its swallow-via-`console.error` for hook errors (observation only). Tx lifecycle errors propagate.

## CLI subcommand additions

Two new subcommands; same parser pattern as Task 47:

```bash
cliq tx validate <txId>              [--json] [--headless]
cliq tx approve  <txId>              [--override <name>]... [--override-all] [--allow-validator-error <name>]... [--reason "..."] [--json] [--headless]
```

`tx-validate` ParsedArgs:
```ts
{ cmd: 'tx-validate'; txId: string; json?: boolean; headless?: boolean }
```

`tx-approve` ParsedArgs:
```ts
{
  cmd: 'tx-approve';
  txId: string;
  overrides: string[];
  overrideAll?: boolean;
  allowValidatorError: string[];
  reason?: string;
  json?: boolean;
  headless?: boolean;
}
```

`cliq tx approve` semantics:
- `--override <name>` per-failed-validator override; repeatable.
- `--override-all` overrides every blocking failure (still requires `--reason`).
- `--allow-validator-error <name>` permits validators with `status: 'error'` (not `fail`) to be treated as pass.

Handler invokes `coordinator.approveTx(txId, { overrides, overrideAll, allowValidatorError, reason })`. On success, transitions tx to `approved`. On failure (uncovered blocking failures), returns structured error. JSON output mirrors existing Task 47 patterns.

## Configuration validation (A.7)

`src/workspace/config.ts` parser checks at the end of `transactions` block resolution:

```ts
if (txCfg.applyPolicy === 'manual-only' && txCfg.auto === 'per-turn') {
  throw new Error('transactions.applyPolicy=manual-only requires transactions.auto=manual; per-turn auto-open with manual-only apply produces dangling transactions');
}
```

Same check applied at CLI flag-resolution time when `--tx-apply manual-only` and config (or default) has `auto: per-turn`.

## Testing

### Unit tests

| File | Tests |
|---|---|
| `src/runtime/tx-runner.test.ts` | Open at turn start emits `tx-staging-start { trigger: 'auto-turn' }`. End-of-turn `auto-on-pass` without blocking failures runs full pipeline; events ordered correctly. End-of-turn `auto-on-pass` with blocking failures aborts with reason='validator-fail'. Explicit tx (auto: manual): runner reuses existing tx, runs the turn body, does NOT finalize/validate/apply at turn end. Interactive prompt path with stub `confirmApply`: yes → applies; no → aborts with reason='user-abort'. Headless+interactive: `createRunner` throws. |
| `src/workspace/transactions/coordinator.test.ts` | `finalizeTx` writes diff.json, transitions state. `validateTx` builds registry, materializes staged view, persists results. `approveTx` blocks on blocking failures unless overrides; transitions to approved. `recoverAtStart` filters cross-session orphans, processes own-session orphans. |
| `src/workspace/transactions/bash-effects.test.ts` | JSONL append/read symmetry; readBashEffects returns [] on missing. |
| `src/tools/bash.test.ts` (extend) | tx.bashPolicy=passthrough: bash runs, BashEffect appended. tx.bashPolicy=deny: returns error with meta.code='tx-overlay-error'. tx.bashPolicy=confirm + headless: behaves as deny (per-call promote-to-deny, intentional divergence from applyPolicy=interactive). |
| `src/headless/events.test.ts` (extend) | Direct mapping tests for the new error contract (§A.10b): `runtimeEventToHeadless({ type: 'error', stage: 'tool', message, code: 'tx-apply-conflict', recoverable: true })` → headless error with code='tx-apply-conflict' and recoverable=true. Same for `tx-apply-partial` (recoverable=false), `tx-overlay-error`, and cross-session-skipped warnings. Backward-compat case: error event without code/recoverable falls back to existing stage-based mapping with recoverable=false. |

### Integration tests

`src/workspace/transactions/integration.test.ts` extended with:

| Test | What it exercises |
|---|---|
| Full happy path through runner | `createRunner({ transactions })` + scripted ModelClient stub emitting one edit action; verify edit lands on disk + tx-applied record + event sequence (tx-staging-start → finalized → validated → applied). |
| Validator failure aborts at turn end | Stub model emits edit; configure a shell validator that exits 1; verify tx-aborted event with reason='validator-fail' + edit doesn't land. |
| Multi-turn explicit tx via the smart-pipeline CLI handler | `coordinator.openTx({ explicit: true })` → simulated turn 1 (overlay edit) → simulated turn 2 (another overlay edit) → invoke the same handler-driver that powers `cliq tx apply <id>` (state-aware: `finalizeTx` → `validateTx` → `approveTx` → `applyTx`). Verify each stage emits its event, both edits land + single tx-applied record + tx-opened paired by meta.txId. This exercises the explicit-tx CLI path described under "Explicit tx flow" — calling `coordinator.applyTx` directly would fail the Stage A1a guard since the tx is in `staging` after the turn. |

### Closed gaps from prior phases

The `ApplyPartial` test in `apply.test.ts` was a half-test (no orchestrator hook). Runner integration enables a real e2e: stub model → mid-turn race triggers `ApplyPartial` → runner emits `tx-apply-partial` error event. This test moves into the integration suite.

### Deferred

Out-of-scope for this PR (polish PR or future phase):
- Stage C crash convergence integration test (Phase 9 placeholder).
- AB3a.5 race injection.
- Stage B B3b disk-error explicit test.
- `cliq run --jsonl` subprocess-based e2e.
- Pre-existing `extensions/loader.test.ts` /tmp/workspace flake.

## Module structure

```text
src/
├── runtime/
│   ├── runner.ts                     ← modified: accepts transactions?
│   ├── tx-runner.ts                  ← new: turn lifecycle helpers
│   ├── tx-runner.test.ts             ← new
│   ├── bash-policy.ts                ← existing, unchanged
│   └── ...
├── workspace/transactions/
│   ├── coordinator.ts                ← modified: +getActiveTx, +finalizeTx, +validateTx, +approveTx, +recoverAtStart
│   ├── coordinator.test.ts           ← extended
│   ├── bash-effects.ts               ← new
│   ├── bash-effects.test.ts          ← new
│   ├── integration.test.ts           ← extended
│   └── ...
├── tools/
│   ├── types.ts                      ← modified: ToolContext.tx?
│   ├── bash.ts                       ← modified: reads context.tx
│   └── bash.test.ts                  ← extended
├── workspace/
│   └── config.ts                     ← modified: refuses manual-only + per-turn
└── cli.ts                            ← modified: tx-validate, tx-approve handlers + parser
```

## Implementation order

The plan-writing skill will detail per-task ordering. High-level dependency tree:

1. `RuntimeEvent` error variant gains `code?` + `recoverable?` (§A.10b); headless mapper honors them.
2. `bash-effects.ts` (leaf module).
3. `ToolContext.tx` field + `tools/bash.ts` policy enforcement (depends on 2).
4. Coordinator additions (`getActiveTx`, `finalizeTx`, `validateTx`, `approveTx`, `recoverAtStart`) — function-style per §A.10a.
5. `tx-runner.ts` (depends on 1, 3, 4).
6. Workspace config validation refinement (A.7).
7. `runner.ts` integration (depends on 5).
8. CLI: `tx-validate`, `tx-approve` handlers + recovery-at-entry (depends on 4).
9. **Headless boundary plumbing**: extend `HeadlessRunRequest` with optional `txMode`/`txApply` fields (`src/headless/contract.ts`); update both `runHeadless` callsites in `src/cli.ts` to forward `parsed.txMode`/`parsed.txApply`; update `runHeadless` itself in `src/headless/run.ts` to await `recoverAtStart` and build `transactions` from request + workspace config; update interactive `chat` arm in `src/cli.ts` (~L1565) to do the same. Both runner-construction sites emit cross-session warnings. (Depends on 1, 4, 7, 8, and config validation.)
10. Integration tests (depends on 1–9).

Each step ends in a green build + green tests for the modules touched. Final integration suite is the last gate.

## Risks and mitigations

- **Risk**: cross-session recovery footgun. **Mitigation**: A.6 + cross-session-skipped recovery.json + warning event; user gets visibility without state corruption.
- **Risk**: `applyPolicy=interactive` UX in TTY mode is hard to get right (diff preview, validator results display). **Mitigation**: v0.8 ships a minimal Y/n prompt with a one-line summary (`Apply tx_xxx? (N files changed, 0 blocking failures): [y/N]`). Richer review can be added in a follow-up.
- **Risk**: model emits a turn that touches no files; runner still opens implicit tx, generates empty diff. **Mitigation**: `coordinator.finalizeTx` detects `diff.files.length === 0` and short-circuits to abort with reason='user-abort'-equivalent (no validators, no apply). `tx-aborted` emitted with a "no edits" marker. Cleaner than producing an empty applied tx.
- **Risk**: bash tool runs concurrently with the OverlayWriter, both touching the same file. **Mitigation**: documented in design that bash modifies cwd directly while OverlayWriter stages in `<txDir>/overlay`. Stage A's preflight catches conflicts (cwd content !== entry.oldContent) and emits ApplyConflict. The BashEffect record makes the source of the conflict visible. v0.8 doesn't pre-validate intersections; this is left for a future "detect bash-overlay collision" enhancement.

## Acceptance criteria

- `cliq --tx edit "..."` against a clean workspace runs end-to-end: opens implicit tx, edits go through overlay, validators run, apply lands changes, all events emitted.
- `cliq tx open foo && cliq "..." && cliq tx apply <id>` works: explicit tx accumulates across turns, manual apply lands.
- `cliq tx validate <id>` and `cliq tx approve <id>` work as CLI entry points to those coordinator methods.
- `cliq run --jsonl --tx edit "..."` emits `tx-staging-start`, `tx-finalized`, `tx-validated`, `tx-applied` (or `tx-aborted`) in the right order.
- Headless run with `--tx-apply interactive` errors out cleanly at construction with the documented message.
- Bash tool under `bashPolicy=passthrough` records `BashEffect` to `<txDir>/bash-effects.json`.
- All new/modified tests green; build clean.

## Out-of-scope follow-ups (next polish PR)

- `cliq tx diff <id>` / `cliq tx show <id>` / `cliq tx validators` / `cliq tx help`.
- Stage C crash convergence integration test.
- AB3a.5 race injection test.
- Stage B B3b disk-error explicit test.
- `cliq run --jsonl` subprocess e2e.
- A2 index-clean cross-stage recheck (deferred to v0.9).
- Bash-overlay collision pre-validation.
